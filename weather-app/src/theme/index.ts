import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#b8c1ec',
      dark: '#b8c1ec',
    },
    secondary: {
      main: '#b8c1ec',
    },
    background: {
      default: '#232946',
      paper: '#232946',
    },
    text: {
      primary: '#b8c1ec',
      secondary: '#b8c1ec',
    },
  },
});

export default theme;
